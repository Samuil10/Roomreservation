// ----------------------------------------------------
// Admin.java - клас за администратор (наследява Person)
// ----------------------------------------------------

public class Admin extends Person {

    // Конструктор
    public Admin(int id, String name, String username, String password) {
        super(id, name, username, password);
    }
}